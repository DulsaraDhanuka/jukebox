name: library_repository
description: A repository that handles files related requests
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.9.0

dependencies: 
  library_api:
    path: ../library_api

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.26.3
  very_good_analysis: ^10.0.0
